@extends('layouts.app')
@section('title')
{{ $title }}
@endsection
@section('content')
<h1 class="pm-title pm-mt-0">{{ $heading }}</h1>
<div class="pm-card">
<p>{!! $intro !!}</p>
@if(count($features) > 0)
<ul>
@foreach($features as $feature)
<li>{{ $feature }}</li>
@endforeach
</ul>
@endif
</div>
@endsection
