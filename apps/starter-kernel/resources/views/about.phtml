@extends('layouts.app')
@section('title')
{{ $title }}
@endsection
@section('content')
<h1>{{ $heading }}</h1>
<p>{!! $intro !!}</p>
@if(count($features) > 0)
<ul>
@foreach($features as $feature)
<li>{{ $feature }}</li>
@endforeach
</ul>
@endif
@endsection
