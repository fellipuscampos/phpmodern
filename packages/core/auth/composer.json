{
    "name": "phpmodern/auth",
    "description": "Session-backed authentication: PasswordHasher (a typed face on password_hash/verify), Session (a testable wrapper around $_SESSION), and Auth (login/logout/id/check, with session-fixation protection). Fetching the actual user record is left to the caller — this only tracks who is logged in.",
    "type": "library",
    "license": "MIT",
    "require": {
        "php": "^8.2"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.5"
    },
    "autoload": {
        "psr-4": {
            "PhpModern\\Auth\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "PhpModern\\Auth\\Tests\\": "tests/"
        }
    }
}
