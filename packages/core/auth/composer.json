{
    "name": "phpmodern/auth",
    "description": "Session-backed authentication (PasswordHasher, Session, Auth) plus Sanctum-style API tokens (ApiTokenManager, ApiTokenMiddleware) for routes with no session at all. Fetching the actual user record is left to the caller — this only tracks who is logged in.",
    "type": "library",
    "license": "MIT",
    "require": {
        "php": "^8.2",
        "phpmodern/orm": "*",
        "phpmodern/http": "*"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.5"
    },
    "autoload": {
        "psr-4": {
            "PhpModern\\Auth\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "PhpModern\\Auth\\Tests\\": "tests/"
        }
    }
}
