{
    "name": "phpmodern/storage",
    "description": "A file storage abstraction (Filesystem) with a LocalFilesystem implementation — put/get/exists/delete/url behind one interface, so swapping where files live later doesn't mean rewriting every call site.",
    "type": "library",
    "license": "MIT",
    "require": {
        "php": "^8.2"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.5"
    },
    "autoload": {
        "psr-4": {
            "PhpModern\\Storage\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "PhpModern\\Storage\\Tests\\": "tests/"
        }
    }
}
