{
    "name": "phpmodern/mail",
    "description": "Message + Mailer interface, with a LogMailer for local dev (writes to a file instead of actually sending) and a real minimal SmtpMailer (EHLO/AUTH LOGIN/MAIL FROM/RCPT TO/DATA over a plain socket, no external library). The prerequisite for password reset and email verification.",
    "type": "library",
    "license": "MIT",
    "require": {
        "php": "^8.2"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.5"
    },
    "autoload": {
        "psr-4": {
            "PhpModern\\Mail\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "PhpModern\\Mail\\Tests\\": "tests/"
        }
    }
}
